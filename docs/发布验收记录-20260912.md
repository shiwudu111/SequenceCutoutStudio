# 序列预览优化发布验收记录

归档日期：2026-09-12。结论：用户确认“验收通过”，本次优化及对应候选包收口。

## 发布包标识

- 版本：`0.4.0-internal.1`，本次以时间戳区分同版本构建，不替换历史发布标签。
- 文件：`SequenceCutoutStudio-Internal-v0.4.0-internal.1-win-x64-20260912-213330-310.zip`。
- 本地路径：`release/` 下同名文件。
- 大小：579,638,170 bytes，脚本显示 552.79 MB（按 1,048,576 bytes 换算）。
- 构建完成时间：2026-09-12 21:36:13 +08:00；终端总耗时约 164.5 秒。
- SHA-256：`AA1C3A5F244046FD715275D7E757C6D9A04D0EEAF04318A1865F68EA1A792EFD`。归档时重新计算，与构建摘要一致。
- 原始记录：`release/build-internal-20260912-213330-310.log` 和同名 `.json`。

## 本次优化

1. GIF / WebP 导入与原始拆帧：FPS、时长置灰，完整输出 PNG；保留源素材透明度及帧时长，重复拆分新建目录。
2. WebP 半透明保留：用户 512×512、18 帧样本逐帧 RGBA 和时长比对通过，不额外去背景。
3. 序列播放：原始节奏与手动 FPS 切换，区间循环含两端；数字输入在失焦或 Enter 后校验。
4. 放大预览：滚轮隔离页面滚动；每次打开背景开关默认关闭；背景原始尺寸显示；保留缩放、拖动和播放。
5. 顶栏精简：勾选前后样式统一，背景选项收入下拉框；主预览选择方式不变。
6. 固定打包入口：`一键打包.cmd` 复用构建脚本，执行预检查、构建、运行时验证、launcher 编译、中文说明复制和压缩；保留旧 ZIP，记录日志、大小与哈希。

## 验收依据与边界

- 构建记录显示类型检查、构建、launcher 图标、运行时目录、rembg / onnxruntime / PIL+numpy / WebP 支持检查通过。
- 此前 GIF / WebP 解码、独立 Electron 交互及大、小窗口截图验证通过。
- 用户在本次 ZIP 构建完成后明确确认验收通过，本记录将相关“待用户复测/待打包”状态更新为完成。
- 用户未提供本次测试机器和逐项回归明细，因此不额外声称已完成新版干净电脑测试或全部去背景素材质量验收。原始构建 JSON 中的 pending 是构建时状态，保留不改，以本记录追加后续人工验收结论。
- 独立纯色背景抠图 skill 不属于本轮应用集成成果；复杂背景和特效去背景仍有已知限制。
- 开发环境缓存权限提示、原有 IPC 启动监听异常未由本次验收证明已修复，保留为待排查问题。

## 归档操作

本次只更新文档和核对 ZIP 哈希，没有修改功能、重打包、提交 Git、推送或创建发布标签。

下一步建议见 [首批内测反馈与后续迭代计划](首批内测反馈与后续迭代计划.md) 中的 2026-09-12 规划草案，尚未开始实现。
